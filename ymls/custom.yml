environment: ${file(./ymls/environment.yml)}
functions:   ${file(./ymls/functions.yml)}
resources:   ${file(./ymls/resources.yml)}

NODE_ENV:
  dev: dev
  hml: hml
  prd: prd

USERS_TABLE_NAME:
  dev: users-dev
  prd: users-prd

PROFILES_TABLE_NAME:
  dev: profiles-dev
  prd: profiles-prd

PROFILE_BUCKET_NAME:
  dev: ifkpass-profile-photos-dev
  prd: ifkpass-profile-photos-prd
